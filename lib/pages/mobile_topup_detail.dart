import 'package:flutter/material.dart';

class MobileTopUpDetail extends StatelessWidget {
  const MobileTopUpDetail({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: const Color(0xff224269),
        title: const Text('Mobile Top up Detail'),
      ),
    );
  }
}
